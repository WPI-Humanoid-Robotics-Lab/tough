# data is an array of size 4 of float32 data type.
# It contains RGBA values in that order.
# Each component in data is normalized (by 255). 
float32[4] data         # array for r,g,b,a data
float32[3] dataPose   # array for pose
